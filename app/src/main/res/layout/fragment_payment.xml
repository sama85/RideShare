<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/gradient_bg">

    <EditText
        android:id="@+id/editTextCardNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Card Number"
        android:inputType="number"
        android:maxLines="1"
        android:layout_marginTop="30dp" />

    <EditText
        android:id="@+id/editTextCardholderName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Cardholder Name"
        android:layout_marginTop="30dp"
        android:maxLines="1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="30dp">

        <EditText
            android:id="@+id/editTextExpiryDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Expiry Date (MM/YYYY)"
            android:inputType="number"
            android:maxLines="1" />

        <EditText
            android:id="@+id/editTextSecurityCode"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Security Code"
            android:inputType="number"
            android:maxLines="1" />

    </LinearLayout>

    <!-- Add fields for billing address if needed -->
    <!-- Example: EditTexts for address, city, state, postal code, country -->

    <Button
        android:id="@+id/buttonMakePayment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/main"
        android:layout_marginTop="30dp"
        android:text="Make Payment" />

</LinearLayout>
